# asgn_5 ->
Assignment 5

 Task 1
     Created a Dictionary of student with name as key and their marks as values.
     Taking name from user -> user_name (whose mark user wants to know)
     TRY:
         if the name of student exist in dictionary 
           then if the student having some marks we tell their marks otherwise will tell them student does not exist.

      Except:
        if the name of student is not in dictionary
        we will tell them to use correct name and also show their used name.


Task 2
   A list with value from 1 to 10 stored in list(variable)
    first five elements now stored in first_five variable.
    now the extracted elements are stored after reversing into rev_ff variable.

    print each variable one by one with the most suitable discription.
